A role names a job — a heading, a hairline, a danger state — and resolves to one ink
or one ramp stop. Nothing in a renderer reads an ink directly; it asks for the role.

{{section:ds-roles}}

The same parsed roles drawn as a diagram, so the table above and this map can never
disagree about which ink backs which role.

{{section:ds-binding-map}}
