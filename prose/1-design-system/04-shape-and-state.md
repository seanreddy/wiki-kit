Radii, overlaps and the hairline rules that separate one block from the next are all
drawn at their real pixel values — a radius here is a radius you can measure.

{{section:ds-shape}}

Every interactive state a component can be in — enabled, disabled, pressed, selected,
locked — is a small fixed set, shown here as it actually renders.

{{section:ds-states}}
