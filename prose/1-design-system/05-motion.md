Motion is timing, not choreography: a small fixed set of durations that every
transition on the site picks from, running here at their true speed.

{{section:ds-motion}}

{{notes:begin}}
Motion tokens are durations only — milliseconds, nothing else. Easing, direction and
distance are the renderer's decision at the point of use, not a token.
{{notes:end}}
