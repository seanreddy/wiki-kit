{{lede:Unproven ideas, parked with a status tag}}

# Ideas

An idea lives here until it is either promoted into a real page or dropped. Each card
is a heading, a status line — `seed`, `exploring` or `promoted` — and a body. Unknown
statuses fail the build.

{{section:id-index}}

## Keep the Example page as a pattern library
*status: seed*

The Example page shows every diagram idiom once. It could stay as a permanent
pattern library instead of being deleted after adoption. Explore: does a
permanent page tempt authors to write diagram-shaped prose?
